spring:
  jmx:
    enabled: true

jmx-metrics-export:
  enabled: true
  prefix: jmx
  scrape-interval: 1000
  config-files: classpath:mbean-metrics-config.json, classpath:composite-metrics-config.json